#  Лабораторная работа №3. Аутентификация и авторизация

## Цель работы

Целью данной лабораторной работы было реализовать систему **аутентификации и авторизации** в REST API-приложении на Node.js с использованием JWT.
В процессе я научилась создавать регистрацию, вход пользователей, ограничивать доступ к маршрутам по ролям и использовать JWT-токены для защиты данных.


##  Стек технологий

| Технология                     | Назначение           |
| ------------------------------ | -------------------- |
| **Node.js + Express**          | серверная логика     |
| **PostgreSQL + Sequelize ORM** | база данных          |
| **JWT (jsonwebtoken)**         | аутентификация       |
| **bcrypt**                     | хэширование паролей  |
| **dotenv**                     | переменные окружения |
| **Swagger**                    | документирование API |


##  Структура проекта

```
todo-app/
 ├── controllers/
 ├── middleware/
 ├── models/
 ├── routes/
 ├── migrations/
 ├── app.js
 ├── .env
 ├── package.json
 └── README.md
```

<img width="657" height="655" alt="image" src="https://github.com/user-attachments/assets/69184960-fca7-4a55-8d18-bdaf3c43d3e4" />

##  Шаг 1. Создание структуры базы данных

Я добавила новую таблицу `users` и поле `user_id` в таблицу `todos`, чтобы каждая задача принадлежала конкретному пользователю.

**Таблица users:**

| Поле       | Тип          | Описание                             |
| ---------- | ------------ | ------------------------------------ |
| id         | SERIAL       | Уникальный идентификатор             |
| username   | VARCHAR(50)  | Имя пользователя                     |
| email      | VARCHAR(100) | Уникальный email                     |
| password   | TEXT         | Захэшированный пароль                |
| role       | VARCHAR(20)  | Роль пользователя (`user` / `admin`) |
| created_at | TIMESTAMP    | Дата регистрации                     |
| updated_at | TIMESTAMP    | Дата обновления                      |

<img width="1311" height="339" alt="image" src="https://github.com/user-attachments/assets/5057e80a-8cfe-4727-b3f6-7d00dbc3188d" />

<img width="1310" height="336" alt="image" src="https://github.com/user-attachments/assets/d26cc07a-8617-46d2-a21c-e876d7018cb1" />


##  Шаг 2. Реализация аутентификации

Созданы маршруты:

| Метод | URL                  | Описание                        |
| ----- | -------------------- | ------------------------------- |
| POST  | `/api/auth/register` | Регистрация нового пользователя |
| POST  | `/api/auth/login`    | Вход пользователя               |
| GET   | `/api/auth/profile`  | Получение данных по токену      |

---

###  Регистрация

Перед регистрацией я проверяю, что email уникален. Пароль хэшируется через `bcrypt` и сохраняется в базе.

![register users](screenshots/register-users.png)
<img width="829" height="676" alt="image" src="https://github.com/user-attachments/assets/888bc307-3c19-4f65-b6e9-fff3673c5ffe" />
<img width="818" height="651" alt="image" src="https://github.com/user-attachments/assets/80e6b6d1-7123-45e7-aa70-a49226ba3a7b" />


###  Вход и выдача токена

После входа создаётся JWT-токен с информацией о пользователе (id, username, role), который хранится на клиенте и передаётся в заголовке при каждом запросе.

<img width="840" height="649" alt="image" src="https://github.com/user-attachments/assets/ef046df9-2e0e-43e8-b70c-dddd872b28d0" />

##  Шаг 3. Реализация авторизации

Создан middleware `authMiddleware.js`, который проверяет наличие и валидность токена.
Также добавлены проверки ролей:

* `isAdmin` — доступ только администраторам;
* `isOwnerOrAdmin` — доступ владельцу ресурса или администратору.

<img width="756" height="978" alt="image" src="https://github.com/user-attachments/assets/5abf8d35-98ea-4899-8642-93ce3084629e" />

##  Шаг 4. Проверка доступа по ролям

###  Пользователь `user`

| Метод                      | URL              | Результат |
| -------------------------- | ---------------- | --------- |
| POST /api/todos            | ✅ Создаёт задачу |           |
| DELETE /api/todos/:id      | 🚫 403 Forbidden |           |
| DELETE /api/categories/:id | 🚫 403 Forbidden |           |


<img width="820" height="704" alt="image" src="https://github.com/user-attachments/assets/6a180c93-d97d-43d0-90d1-b67e07618a98" />

###  Пользователь `admin`

| Метод                      | URL                          | Результат |
| -------------------------- | ---------------------------- | --------- |
| GET /api/todos             | ✅ Доступ ко всем задачам     |           |
| DELETE /api/todos/:id      | ✅ Разрешено                  |           |
| DELETE /api/categories/:id | ✅ Разрешено (204 No Content) |           |

<img width="841" height="513" alt="image" src="https://github.com/user-attachments/assets/df776364-61f9-476d-b6a2-bb403ef11356" />

<img width="822" height="545" alt="image" src="https://github.com/user-attachments/assets/23ce1571-f1fb-4bac-b4d2-4cd502af0f01" />


##  Шаг 5. Swagger-документация

Для удобного тестирования и демонстрации API я подключила Swagger.
Он доступен по адресу:
 [http://localhost:3000/api-docs](http://localhost:3000/api-docs)


##  Шаг 6. Проверка и демонстрация

Зарегистрировано два пользователя:

*  [admin@example.com](mailto:admin@example.com) — роль `admin`
*  [user@example.com](mailto:user@example.com) — роль `user`

Проверено:
* Авторизация по токену работает 
* Без токена запросы возвращают `401 Unauthorized` 
* Ролевая авторизация выполняется корректно 
  

##  Контрольные вопросы

### 1️ Что такое JWT и как он работает?

**JWT (JSON Web Token)** — это стандарт, который используется для безопасной передачи данных между клиентом и сервером.
После входа сервер создаёт токен с зашифрованными данными (id, username, role).
Клиент сохраняет токен (в localStorage или cookies) и при каждом запросе отправляет его в заголовке:

```
Authorization: Bearer <токен>
```

Сервер проверяет подпись токена и, если она валидна, разрешает доступ к API.

### 2️ Как реализовать безопасное хранение паролей пользователей?

Пароли нельзя хранить в открытом виде.
Перед сохранением в базу они хэшируются при помощи **bcrypt**:

```js
const hash = await bcrypt.hash(password, 10);
```

А при входе сравниваются с помощью:

```js
await bcrypt.compare(password, hash);
```

Таким образом, даже при взломе базы злоумышленник не узнает исходный пароль.

### 3️ В чём разница между аутентификацией и авторизацией?

| Понятие            | Определение                                                                                                            |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| **Аутентификация** | Процесс проверки личности пользователя (например, вход по логину и паролю).                                            |
| **Авторизация**    | Определяет, что именно пользователь может делать (например, админ может удалять задачи, а обычный пользователь — нет). |


### 4️ Какие преимущества и недостатки использования Passport.js для аутентификации в Node.js?

**Преимущества:**

* предоставляет готовые стратегии (JWT, OAuth, Google, GitHub и др.);
* гибкая настройка под разные типы входа;
* простая интеграция с Express.

**Недостатки:**

* требует дополнительной конфигурации;
* избыточен для простых REST API;
* сложнее отлаживать, чем ручная реализация через `jsonwebtoken`.

##  Вывод

В ходе лабораторной работы я:

* реализовала **регистрацию и вход пользователей**;
* настроила **JWT-аутентификацию**;
* добавила **middleware для проверки токенов и ролей**;
* ограничила доступ к API в зависимости от роли пользователя;
* подключила **Swagger-документацию**;
* протестировала систему безопасности через **Postman**.
